# ⚠️ AI HANDOFF INSTRUCTIONS: FRONTEND TO BACKEND INTEGRATION

**To the AI Assistant/Developer reading this file in the Backend IDE:**
Hello! This is a contextual handoff document generated by the Frontend AI Assistant. The user wants to integrate the cutting-edge React frontend we just built with the existing Python FastAPI backend (GitMax). 

This file is your complete roadmap to understand the structural strategy we have agreed upon. The goal is a **Constructive, Synergistic Merging** where both systems enhance each other. We are building a comprehensive Predictive Engineering Intelligence platform.

---

## 1. The Core Architecture Philosophy
The Frontend is a massive **Enterprise Engineering Dashboard** featuring PR risk analysis, repository health sensing, test coverage insights, and executive reports.

Our strategy is progressive enhancement:
- **Phase 1 (Current Focus):** Integrate the backend's core strength: the `POST /api/analyze-pr` intelligent pipeline. We will use this to power the main dashboard and PR pages.
- **Phase 2 & Beyond:** Incrementally expand the Backend to support the other rich frontend features (Insights, Reports, Repository Syncing) as outlined in the overarching problem statement.

We will **not** aggressively delete or discard frontend features just because the backend hasn't implemented them yet. Instead, we use graceful degradation and mock data for pending features until the backend catches up.

## 2. The Data Contract & Mappings (Phase 1: PR Analysis)
The Backend currently exposes `POST /api/analyze-pr` using the Pydantic `AnalysisResponse` schema. We have specifically built the React UI widgets to consume this payload for the Pull Requests section.

Here is how the UI is expecting to map the data you provide:

*   **`summary.overall_risk`** ➔ Powers the `<RiskBadge>` components (High/Medium/Low rendering).
*   **`summary.confidence`** ➔ Powers the animated `<RiskScoreRing>` circular charts.
*   **`agent_logs` (Array)** ➔ Powers the streaming "AI Agent Activity" feed on the Dashboard. *Future enhancement: Exposing this via WebSockets/SSE would make the UI pulse dynamically.*
*   **`files` [FileResult]** ➔ Powers the tables displaying complexity metrics and cost-loss predictions for each file changed in the PR.

## 3. What We Are Building on the Frontend Right Now
1.  **The Input Layer:** I will build a global `AnalyzePRModal.jsx` where the user inputs the `repo_url` and `pr_number` required by your POST request.
2.  **API Client Setup:** I am configuring `axios` and `@tanstack/react-query` to hit `localhost:8000/api`.
3.  **Graceful Feature Toggles:** Setting up logic so that if the backend missing a specific endpoint (like Insights or Reports), the frontend will seamlessly display "Coming Soon" or cached mock states, without crashing.

## 4. Your Checklist (Backend Integration Objectives)
To ensure the React app can talk to your FastAPI app without violently crashing, please verify the following on your end:

- [ ] **Enable CORS:** Ensure `fastapi.middleware.cors.CORSMiddleware` is configured in `main.py` specifically allowing `http://localhost:5173` and `http://localhost:5174` (our Vite dev servers).
- [ ] **Graceful Formatting:** Validate that dates are serialized as standard ISO 8601 strings, and enums (like Risk Levels) are consistently capitalized (e.g., `HIGH`, `MEDIUM`).
- [ ] **Graceful Degration:** Ensure that if the ML model fails internally on a specific file, the API still returns a valid `200` JSON response containing the success parameters for other files, rather than throwing a hard 500 server error that will crash the beautiful UI loading states.
- [ ] **Future Expansion Prep:** Keep in mind that future sprints will require endpoints for fetching overall system health, top risky repos, and historical blind spots.

## Final Note
Our guiding principle is **Isolation vs Synergy**. If the backend doesn't have a feature, the frontend will preserve its design and wait gracefully. Please ensure the backend API behaves distinctly and cleanly!
