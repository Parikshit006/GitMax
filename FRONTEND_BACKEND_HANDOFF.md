# ⚠️ AI HANDOFF INSTRUCTIONS: FRONTEND TO BACKEND INTEGRATION

**To the AI Assistant/Developer reading this file in the Backend IDE:**
Hello! This is a contextual handoff document generated by the Frontend AI Assistant. The user wants to integrate the cutting-edge React frontend we just built with the existing Python FastAPI backend (GitMax). 

This file is your complete roadmap to understand the structural strategy we have agreed upon. The goal is a **Constructive, Synergistic Merging** where both systems enhance each other. Do not overwrite the frontend structure; instead, align the backend to fuel it.

---

## 1. The Core Architecture Philosophy
The Frontend has been heavily redesigned to be an **AI PR Analysis Visualizer**. We have aggressively stripped away generic enterprise dashboard features (like weekly testing trends or developer burnout reports) because the Backend's problem statement is specifically focused on the `POST /api/analyze-pr` intelligent pipeline.

Our goal is to make the Backend AI Orchestrator feel instantaneous and premium on the frontend.

## 2. The Data Contract & Mappings
The Backend exposes `POST /api/analyze-pr`. It uses the Pydantic `AnalysisResponse` schema. We have specifically built the React UI widgets to consume this exact payload.

Here is how the UI is expecting to map the data you provide:

*   **`summary.overall_risk`** ➔ Powers the `<RiskBadge>` components (High/Medium/Low rendering).
*   **`summary.confidence`** ➔ Powers the animated `<RiskScoreRing>` circular charts.
*   **`agent_logs` (Array)** ➔ Powers the streaming "AI Agent Activity" feed on the Dashboard. *Future enhancement: Exposing this via WebSockets/SSE would make the UI pulse dynamically.*
*   **`files` [FileResult]** ➔ Powers the tables displaying complexity metrics and cost-loss predictions for each file changed in the PR.

## 3. What We Are Building on the Frontend Right Now
If you are reading this, the user is likely preparing the backend environment. Here is what I (the Frontend Assistant) am actively building to meet you halfway:
1.  **Cutting Code:** I am deleting the `Insights.jsx` and `Reports.jsx` pages entirely.
2.  **The Input Layer:** I am building a global `AnalyzePRModal.jsx` where the user inputs the `repo_url` and `pr_number` required by your POST request.
3.  **API Client:** I am configuring `axios` and `@tanstack/react-query` to hit `localhost:8000/api` and handle the loading states gracefully.

## 4. Your Checklist (Backend Integration Objectives)
To ensure the React app can talk to your FastAPI app without violently crashing, please verify the following on your end:

- [ ] **Enable CORS:** Ensure `fastapi.middleware.cors.CORSMiddleware` is configured in `main.py` specifically allowing `http://localhost:5173` and `http://localhost:5174` (our Vite dev servers).
- [ ] **Graceful Formatting:** Validate that dates are serialized as standard ISO 8601 strings, and enums (like Risk Levels) are consistently capitalized (e.g., `HIGH`, `MEDIUM`).
- [ ] **Graceful Degration:** Ensure that if the ML model fails internally on a specific file, the API still returns a valid `200` JSON response containing the success parameters for other files, rather than throwing a hard 500 server error that will crash the beautiful UI loading states.

## Final Note
Our guiding principle is **Isolation vs Synergy**. If the backend doesn't have a feature, the frontend will use Feature Toggles to hide the UI gracefully. Please ensure the backend API behaves distinctly and cleanly!
